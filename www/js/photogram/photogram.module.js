(function (window, angular, undefined) {
    'use strict';
    angular
        .module('app.photogram', [
            'ionic',
            'ngCordova',
            'gettext'
        ]);
})(window, window.angular);
